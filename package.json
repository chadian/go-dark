{
  "name": "go-dark",
  "author": "Chad Carbert (@chadian)",
  "version": "0.1.0",
  "description": "An app for managing mac notifications",
  "main": "app/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "yarn build && electron ./app/index.js",
    "release": "yarn build && electron-builder"
  },
  "build": {
    "appId": "com.sticksnglue.go-dark"
  },
  "license": "ISC",
  "dependencies": {
    "@sindresorhus/do-not-disturb": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^12.12.5",
    "electron": "^11.5.0",
    "electron-builder": "^22.1.0",
    "typescript": "^3.6.4"
  }
}
